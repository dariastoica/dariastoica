/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add remote domains here later if you host images externally
    remotePatterns: [],
  },
};

module.exports = nextConfig;
